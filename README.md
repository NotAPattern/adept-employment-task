# To do

- [ ] Delete company
- [ ] Delete employee
- [ ] Pagination company
- [ ] Pagination employee

# Install and run

- npm: `npm install && npm build && npm preview`
- yarn: `yarn install && yarn build && yarn preview`
